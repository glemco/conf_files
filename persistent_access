Defaults    env_keep += "EDITOR PATH"

## Allows to use password only once for wheel group
#Defaults:%wheel !tty_tickets,timestamp_timeout=500

## Allows to enter commands without password
%wheel	ALL=(ALL)	NOPASSWD: ALL
